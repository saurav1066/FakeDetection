[project]
name = "fakes-detection"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "saurav1066", email = "sauravadhikari35@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.5",
    "scikit-learn>=1.9.0",
]

[project.scripts]
fakes-detection = "fakes_detection:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
